<UserControl x:Class="Maptz.Subtitler.Wpf.Controls.VideoPlayer.SubtitleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Border x:Name="x_Border_Top"  Grid.Row="1" BorderBrush="White"></Border>
        </Grid>

        <Border x:Name="x_Border_Bottom"  Grid.Row="1" BorderBrush="White"></Border>
        <!--<l2:TouchTextControl Grid.Row="1" x:Name="x_TouchTextControl" Margin="10"></l2:TouchTextControl>-->
        
    </Grid>
</UserControl>
